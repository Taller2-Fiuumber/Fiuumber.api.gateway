version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: "sh"
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    ports:
      - "1234:1234"
    expose:
      - 1234
      - 8080
    volumes:
        - ./:/app
    environment:
        PORT: 1234
    networks:
      - default

networks:
  default:
    name: localdev

volumes:
  database_data:
    driver: local
